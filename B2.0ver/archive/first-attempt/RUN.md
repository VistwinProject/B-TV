# 啟動 2.0

目錄：`/Users/chunming/B2.0ver`，預覽網址：`http://127.0.0.1:5284/`。

雙擊 `啟動-B2.0.command` 或執行 `npm run dev`。第一次安裝請執行 `npm ci`。停止時於啟動終端按 Ctrl+C。

按 C 開始邀請流程，按 1–5 切換情境，N 提前進入選項，R 回待機。也可展開左下「替代 NFC 卡片」。

完整流程、配音與 NFC 設定見 README.md。此版本的預設本機模式不連線至初代 NFC server。
